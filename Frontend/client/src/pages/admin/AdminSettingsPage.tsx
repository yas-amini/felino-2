import AdminPage from "../../components/admin/AdminPage";

export default function AdminSettingsPage() {
  return (
    <AdminPage title="Inställningar">
      <p>Här kommer inställningar för admin.</p>
    </AdminPage>
  );
}